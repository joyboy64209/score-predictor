# Backend / DB
POSTGRES_USER=predictor
POSTGRES_PASSWORD=64209
POSTGRES_DB=match_predictor
DATABASE_URL=postgresql://predictor:64209@localhost:5432/match_predictor?schema=public

# Auth
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=7d

# Service wiring
ML_SERVICE_URL=http://localhost:8000
PORT=3001
FRONTEND_URL=http://localhost:3000

# ML
ML_DATABASE_URL=postgresql://predictor:predictor@localhost:5432/match_predictor?schema=public
BACKEND_URL=http://localhost:3001

# Provider API keys (never hardcode - use environment variables)
FOOTBALL_DATA_API_KEY=
API_FOOTBALL_API_KEY=
KAGGLE_USERNAME=
KAGGLE_KEY=

# HTTP behaviour
HTTP_TIMEOUT=20
HTTP_MAX_RETRIES=3
HTTP_CACHE_TTL=3600

# Confidence thresholds (configurable, editable at runtime via /admin/config)
THRESHOLD_MATCH_RESULT=70
THRESHOLD_DOUBLE_CHANCE=90
THRESHOLD_OTHER=80
THRESHOLD_COMBINATION=80
